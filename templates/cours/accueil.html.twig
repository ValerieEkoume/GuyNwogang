{% extends 'layouts/base.html.twig'%}

{% block title 'Joue Alors' %}

{% block body %}




    <div class="jumbotron text-center">
        <h1>Joue-Alors !</h1>
        <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. A adipisci culpa cupiditate deserunt explicabo
            harum, incidunt inventore ipsam iure labore laboriosam nesciunt numquam provident quae quos soluta totam veniam voluptates?</p>
    </div>


        <h2 class="mb-4 text-center">Les nouveautés</h2>


            <div class="row mb-5">
                {% for cours in courses  %}
                    <div class="col-md-4">
                        <article class="cours bg-white rounded shadow">
                            <div class="mw-100 overflow-hidden">
                                <a href="{{ path('cours.show', {id: cours.id, slug: cours.slug}) }}">
                                    <img src="{{ (cours.imageName ? vich_uploader_asset(cours) :
                                    asset('build/images/GN_thumbnail.ef61ec6f.jpg')) |
                                    imagine_filter('squared_thumbnail_medium')}}" alt="{{ cours.title }}"
                                         class="w-100 rounded-top"/>
                                </a>
                            </div>


                            <h3 class="pl-3 pt-3"><a href="{{ path('cours.show', {id: cours.id, slug: cours.slug}) }}"
                                    class="text-muted text-decoration-none">{{ cours.title }}
                                </a></h3>
                            <p class="pl-3 pb-3 text-muted">{{ cours.genre }} ({{ cours.niveauType }})</p>
                        </article>
                    </div>
                {% endfor %}
            </div>



{% endblock %}

