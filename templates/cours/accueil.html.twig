{% extends 'base.html.twig' %}

{% block title 'Joue Alors!' %}

{% block body %}

    <nav class="navbar navbar-light bg-light">
        <div class="container-fluid">
            <a href="{{ path('app_home_home') }}" class="navbar-brand">Guy Nwognag</a>
            <a href="{{ path('cours.joueAlors') }}" class="navbar-brand">Joue-Alors</a>
            <form class="d-flex">
                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
        </div>
    </nav>

    <div class="container ">
        <h2> Tous les derniers Cours</h2>
        <div class="row flex">
            {% for cours in courses %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <a href="#">{{ cours.title }}</a>

                            </h5>
                            <p class="card-text">{{ cours.genre }} ({{ cours.NIVEAU }})</p>
                            <div class="text-primary">{{ cours.formatedPrix }} €</div>

                        </div>
                    </div>

                </div>

            {% endfor %}

        </div>

    </div>

{% endblock %}

