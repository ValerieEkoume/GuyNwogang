{% extends 'layouts/base.html.twig' %}

{% block title cours.title %}


{% block body %}

    {% for message in app.flashes('success') %}
    <div class="flash-notice">
        {{ message }}
    </div>
    {% endfor %}

    {% if not form.vars.valid %}
    <div class="alert-danger">
        Erreur, les champs n'ont pas été correctement remplis !
    </div>
    {% endif %}

    <div class="titre-show">
        <h2 class="text-center">
            {{ cours.description  | nl2br}}
        </h2>
    </div>

    <div class="container ">

        <div class="row">
            <div class="col">
                <video
                        id="video"
                        class="video-js"
                        controls
                        preload="auto"
                        width="845"
                        height="650"
                        poster="{{ asset('build/images/GN_thumbnail.ef61ec6f.jpg') }}"
                        data-setup="{}"
                >
                    <source src="{{ asset('build/images/1_bikutsiB_bpm100.be32ed6d.mp4') }}" type="video/mp4" />
                    <source src="MY_VIDEO.webm" type="video/webm" />
                    {
                    <p class="vjs-no-js">
                        To view this video please enable JavaScript, and consider upgrading to a
                        web browser that
                        <a href="https://videojs.com/html5-video-support/" target="_blank"
                        >supports HTML5 video</a
                        >
                    </p>
            </video>

            </div>

{#            <div class="col">#}
{#                {% for video in cours.videos %}#}
{#                <ul class="list-group list-group-numbered">#}

{#                    <li class=""><a class="text-decoration-none text-muted" href="{{ video.videoLink }}">{{ video.videoTitle }}</a></li>#}
{#                </ul>#}
{#                {% endfor %}#}
{#            </div>#}
        </div>
    </div>


    {% block javascripts %}
        <script src="path/to/video.js/dist/video.js"></script>
        <script src="path/to/videojs-playlist/dist/videojs-playlist.js"></script>
    {% endblock %}



{% endblock %}